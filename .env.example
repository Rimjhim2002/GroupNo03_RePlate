# Use one shared MongoDB Atlas connection string on every computer running RePlate.
# Do not commit the real URI or its password.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/?retryWrites=true&w=majority
MONGO_DB_NAME=replate
SESSION_EXPIRE_MINUTES=60